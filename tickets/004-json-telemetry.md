# 004 — New machines send JSON

**Reported by:** IT
**Priority:** upcoming

The machines going into the annex building are a newer model. They don't drop
CSV files — they push their events as JSON over HTTP.

We need to be able to take those in too. The annex opens next quarter.
